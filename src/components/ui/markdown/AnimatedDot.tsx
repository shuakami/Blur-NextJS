// components/ui/markdown/AnimatedDot.tsx
import React, { memo } from 'react';

const AnimatedDot: React.FC = () => (
  <span className="ml-2 text-lg text-white z-50">
    ●
  </span>
);

export default memo(AnimatedDot);
